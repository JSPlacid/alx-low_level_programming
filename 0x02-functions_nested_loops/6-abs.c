#include "main.h"
/**
 * _abs - Returns absolute value of an integer
 * @r: Integer input
 * Return: Absolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
